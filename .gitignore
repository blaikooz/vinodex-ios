.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcuserstate

# xtool's build output (the packaged .app)
xtool/

# iOS signing material and secrets — xtool's free-profile deploy handles these
# in this working directory; never committable. (arch R3 / auditS L11)
# *.p8 added 2026-08-11: an App Store Connect API AuthKey was found loose in
# the repo root, one `git add .` away from being published.
*.mobileprovision
*.p12
*.p8
*.cer
*.certSigningRequest
*.ipa
*.dSYM/
.env

# Only needed if you regenerate the bundled data; see README.
node_modules/
scripts/__pycache__/

.DS_Store
*.log

# `npm run outlines` (0.8.4, F1). The ring rasteriser stopped being the master
# of art/icons/countries/ when the hand-drawn set landed, so its output is a
# reference render rather than a source: useful to look at, never shipped, and
# never a diff to review.
art/icons/countries-from-rings/
build.log
