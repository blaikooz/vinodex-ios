# Diagnostics this check tolerates. Everything else fails the run.
#
# Line and column are stripped so an edit above a line does not churn the file.
# Regenerate with: scripts/typecheck-ios-surface.sh --update-baseline
#
# Nothing belongs here just because it is noisy. Each line needs a reason:
#
#  * DexIcon `current` — genuinely macOS-only. `UITraitCollection.current` is
#    fine on iOS; the shim aliases it to `NSAppearance`, whose `current` is
#    deprecated. An artifact of the stand-in, not of the app.
#
#  * DexSearchField (6) — UNRESOLVED, not accepted as correct. `Coordinator`
#    touches `field.text` and `resignFirstResponder()` from `@objc` methods.
#    Whether that is legal on iOS turns on whether the SDK annotates
#    `UITextFieldDelegate` as `@MainActor` — if it does, `Coordinator` is
#    inferred `@MainActor` and all six vanish; if it does not, these are six
#    real CI errors. It cannot be settled without the iOS SDK, and CI has never
#    reached this file: its iOS compile stops after the first batch, which ends
#    at DexIcon.swift. The first CI run that gets past BookmarksScreen settles
#    it. Delete these six lines the moment it does, either way.
#
Sources/VinodexUI/Components/DexIcon.swift: warning: 'current' was deprecated in macOS 12.0: Use -performAsCurrentDrawingAppearance: to temporarily set the drawing appearance, or +currentDrawingAppearance to access the currently drawing appearance. [#DeprecatedDeclaration]
Sources/VinodexUI/Components/DexSearchField.swift: warning: call to main actor-isolated instance method 'resignFirstResponder()' in a synchronous nonisolated context [#ActorIsolatedCall]
Sources/VinodexUI/Components/DexSearchField.swift: warning: main actor-isolated property 'selectedTextRange' can not be mutated from a nonisolated context
Sources/VinodexUI/Components/DexSearchField.swift: warning: main actor-isolated property 'selectedTextRange' can not be referenced from a nonisolated context
Sources/VinodexUI/Components/DexSearchField.swift: warning: main actor-isolated property 'text' can not be mutated from a nonisolated context
Sources/VinodexUI/Components/DexSearchField.swift: warning: main actor-isolated property 'text' can not be referenced from a nonisolated context
